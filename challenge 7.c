#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
void add (int a , int b ){
	int somme ;
}
int main() {
	int a , b , somme;
	printf("la premier valeur : ");
	scanf("%d",&a);
    printf("la deuxieme valeur : ");
	scanf("%d",&b);
	somme = a + b ;
	printf("la somme de deux valeur est %d + %d = %d ",a,b,somme);
	add (a,b);
	return 0;
}
